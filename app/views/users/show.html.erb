<div class="row form user_profile">
  <div class="col-lg-12">
    <div class="image" style="background-image: url(<%= @user.avatar.url %>);"></div>
   <h2><%= "#{@user.account_name}" %></h2>
   <% if current_user.id == @user.id %>
   <div class="profile_edit">
   <%= link_to "プロフィール編集", edit_users_path(current_user),class:"btn btn-primary wtihripple" %>
  </div>
  <% end %>
  </div>
</div>